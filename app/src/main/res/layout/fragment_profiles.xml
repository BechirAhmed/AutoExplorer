<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/colorAppBackground">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/profilesRv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingEnd="2dp"
        android:paddingStart="2dp"/>

    <LinearLayout
        android:id="@+id/profilesEmptyStateLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:layout_width="@dimen/empty_state_view_image_size"
            android:layout_height="@dimen/empty_state_view_image_size"
            android:layout_margin="48dp"
            android:src="@drawable/ic_search_profile_empty_state"
            tools:ignore="ContentDescription"/>

        <auto.explorer.base.ExplorerTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="64dp"
            android:layout_marginStart="64dp"
            android:gravity="center_horizontal"
            android:text="@string/empty_state_no_profiles"
            android:textAlignment="center"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
            android:textColor="@android:color/white"/>
    </LinearLayout>

</FrameLayout>
